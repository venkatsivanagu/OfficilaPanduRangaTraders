{
  "name": "panduranga-traders",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "office_bills"
  ],
  "scripts": {
    "install:all": "npm install && cd office_bills && npm install",
    "start": "cd office_bills && npm run dev",
    "build": "cd office_bills && npm run build",
    "preview": "cd office_bills && npm run preview",
    "test": "cd office_bills && npm test"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "react-datepicker": "^8.4.0"
  }
}
